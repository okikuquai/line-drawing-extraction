// theme/themeConfig.ts
import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  components: {
    Layout: {
      headerHeight: 80,
    },
    Menu: {},
  },
};

export default theme;
